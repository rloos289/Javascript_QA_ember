
import { totalRatings } from 'q-a/helpers/total-ratings';
import { module, test } from 'qunit';

module('Unit | Helper | total ratings');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = totalRatings([42]);
  assert.ok(result);
});

