
import { answerRating } from 'q-a/helpers/answer-rating';
import { module, test } from 'qunit';

module('Unit | Helper | answer rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerRating([42]);
  assert.ok(result);
});

